# ===========================================
# CONFIGURACIÓN DE AGENTES ADK MEMORY
# ===========================================

# API Key de Google AI Studio (para Database y ADK Agent)
# Obtener en: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=AIzaSy...

# ===========================================
# CONFIGURACIÓN DE VERTEX AI (para Vertex Agent)
# ===========================================

# NOTA: Para Vertex Agent, NO uses GOOGLE_API_KEY
# Vertex Agent usa OAuth2 con gcloud auth application-default login

# ID del proyecto de Google Cloud
# Ejemplo: mi-proyecto-123456
GOOGLE_CLOUD_PROJECT=tu_project_id

# Ubicación de Vertex AI (us-central1, europe-west1, etc.)
GOOGLE_CLOUD_LOCATION=us-central1

# ID del Agent Engine (se genera automáticamente con create_agent_engine_vertex.py)
AGENT_ENGINE_ID=tu_agent_engine_id

# ===========================================
# CONFIGURACIÓN DEL SERVIDOR
# ===========================================

# Agente por defecto (database, adk, vertex)
SELECTED_AGENT=vertex

# Modelo de IA a usar
AGENT_MODEL=gemini-2.0-flash

# ===========================================
# CONFIGURACIÓN OPCIONAL
# ===========================================

# Puerto del servidor (opcional, por defecto 8000)
# SERVER_PORT=8000

# Host del servidor (opcional, por defecto localhost)
# SERVER_HOST=localhost
